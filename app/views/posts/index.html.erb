<head>
	<link href='http://fonts.googleapis.com/css?family=PT+Sans+Narrow:400,700' rel='stylesheet' type='text/css'>
				
</head>

<div class="hero-unit">
        <h1>Hammer In The News | Artifice is the New Steel</h1>
        <p>I'd rather be a Hammer than a Nail.  Yes I would.  If I only could.  I surely would.</p>
        <p><a href="#" class="btn btn-primary btn-large">Learn more &raquo;</a></p>
      </div>

      <!-- Example row of columns -->
      <% @posts.each do |post| %>
      <div class="row">
        <div class="span4">
          <h2><%=post.title %></h2>
          <p><%=post.body %></p>
          <p><%= render post.comments %>
          <p><a class="btn" href="#">View details &raquo;</a></p>
        </div>
        <% end %>
      <h2><%= link_to "New Post", new_post_path %></h2>

      <hr>
<!-- Example row of columns -->

      </div>
    

